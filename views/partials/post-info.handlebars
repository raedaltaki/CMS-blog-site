<div class="card text-dark bg-light mx-5 my-2">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h4>{{title}}</h4>
    <p class="m-0">Posted by {{user.username}} on {{format_date createdAt}}</p>
  </div>
  <div class="card-body">
    {{!-- <h5 class="card-title">Special title treatment</h5> --}}
    <p class="card-text">{{body}}</p>
  </div>
</div>