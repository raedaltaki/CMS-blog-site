<ul class="list-group">
  {{#each posts as |post|}}
  <li class="">
    {{> post-info post }}
  </li>
  {{/each}}
</ul>

    {{> new-update-post "Create" }}

<div class="p-3 d-flex justify-content-center align-items-end">
    <button class="btn btn-dark btn-lg"> + New Post</button>
</div>